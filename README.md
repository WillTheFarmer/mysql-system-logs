# mysql-schema-system-logs

## repository rename

## http-logs-to-mysql to log-files-to-MySQL

Now Python re-write and SQL has separate repository there is another database I've been building.

Now additional sub-processes can be added to log-files-to-mysql import process.

Same Typescript Node.js Web interface works on BOTH http and system logs.

database schema snapshot designed for system logs for Linux-based servers with adjustable retention days
